<Window x:Class="Seacore.Resources.Usercontrols.Clients.Windows.Management.ClipboardManagerWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" Height="390" Width="375" UseLayoutRounding="True" TextOptions.TextFormattingMode="Display" Background="Transparent" WindowStyle="None" ResizeMode="NoResize" WindowStartupLocation="CenterScreen" AllowsTransparency="True">

    <Border CornerRadius="5">
        <Border.Background>
            <SolidColorBrush Color="#1B1B1B"/>
        </Border.Background>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="24"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Background="#181818" MouseLeftButtonDown="Border_MouseLeftButtonDown" BorderThickness="0,0,0,1" BorderBrush="#1F1F1F">
                <Grid>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Style="{StaticResource minimizeWindowBTN}" Click="MinimizeButton_Click" Cursor="Hand"/>
                        <Button Style="{StaticResource closeWindowBTN}" Click="CloseButton_Click" Cursor="Hand"/>
                    </StackPanel>
                </Grid>
            </Border>

            <Grid Grid.Row="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="160"/>
                    <RowDefinition Height="12"/>
                    <RowDefinition Height="22"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Border x:Name="clipboardContent" Background="#1F1F1F" Width="350" CornerRadius="2"/>
                <StackPanel Orientation="Horizontal" Grid.Row="2" Margin="12,0,0,0">
                    <Button Width="80" Content="Copy" Background="#1F1F1F" Foreground="White" BorderThickness="0"/>
                    <Button Width="80" Margin="12,0,12,0" Content="Remove" Background="#1F1F1F" Foreground="White" BorderThickness="0"/>
                    <Button Width="80" Content="Replace" Background="#1F1F1F" Foreground="White" BorderThickness="0"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom" Margin="12,0,0,12" Grid.Row="3">
                    <Button Width="80" Height="22" Margin="0,0,12,0" Content="History" Background="#1F1F1F" Foreground="White" BorderThickness="0"/>
                    <Button Width="100" Height="22" Content="Manipulate" Background="#1F1F1F" Foreground="White" BorderThickness="0"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>