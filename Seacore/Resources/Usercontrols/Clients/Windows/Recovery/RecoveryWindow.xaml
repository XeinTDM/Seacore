<Window x:Class="Seacore.Resources.Usercontrols.Clients.Windows.Recovery.RecoveryWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" Height="350" Width="600" UseLayoutRounding="True" TextOptions.TextFormattingMode="Display" Background="Transparent" WindowStyle="None" ResizeMode="NoResize" WindowStartupLocation="CenterScreen" AllowsTransparency="True">

    <Border CornerRadius="5">
        <Border.Background>
            <SolidColorBrush Color="#1B1B1B"/>
        </Border.Background>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="80"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Background="#181818" BorderThickness="0,0,1,0" BorderBrush="#1F1F1F">
                <StackPanel VerticalAlignment="Center">
                    <ToggleButton Width="30" Height="30" x:Name="btnDashboard" Cursor="Hand"/>
                    <ToggleButton Width="30" Height="30" x:Name="btnLogs" Margin="0,15,0,15" Cursor="Hand"/>
                    <ToggleButton Width="30" Height="30" x:Name="btnAnalytics" Cursor="Hand"/>
                </StackPanel>
            </Border>
            
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="24"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Background="#181818" MouseLeftButtonDown="Border_MouseLeftButtonDown" BorderThickness="0,0,0,1" BorderBrush="#1F1F1F">
                    <Grid>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button Style="{StaticResource minimizeWindowBTN}" Click="MinimizeButton_Click" Cursor="Hand"/>
                            <Button Style="{StaticResource closeWindowBTN}" Click="CloseButton_Click" Cursor="Hand"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <Frame x:Name="FrameContent" NavigationUIVisibility="Hidden" VerticalAlignment="Bottom" HorizontalAlignment="Right" Grid.Row="1"/>
            </Grid>
        </Grid>
    </Border>
</Window>