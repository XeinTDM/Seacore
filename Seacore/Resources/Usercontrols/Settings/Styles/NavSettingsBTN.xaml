<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style x:Key="account" TargetType="Button">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="bg" Background="#1E000000" Height="140" Width="160" CornerRadius="5,0,0,0">
                        <Grid>
                            <Image x:Name="icon" Height="70" Width="70" Opacity="0.3" VerticalAlignment="Center" HorizontalAlignment="Center" Source="/Resources/Usercontrols/Settings/Images/[settings]-User.png"/>
                            <TextBlock x:Name="txt" Text="ACCOUNT" Foreground="#fff" FontSize="16" FontWeight="Light" Opacity="0" VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0,0,0,16"/>
                        </Grid>
                    </Border>

                    <ControlTemplate.Triggers>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True"/>
                            </MultiTrigger.Conditions>
                            <MultiTrigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="icon"
                                                         Storyboard.TargetProperty="Opacity"
                                                         To="1" Duration="0:0:0.2"/>

                                        <ColorAnimation Storyboard.TargetName="bg"
                                                        Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                        To="#4C000000" Duration="0:0:0.2"/>

                                        <DoubleAnimation Storyboard.TargetName="txt"
                                                         Storyboard.TargetProperty="Opacity"
                                                         To="1" Duration="0:0:0.2"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </MultiTrigger.EnterActions>
                            <MultiTrigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="icon"
                                                         Storyboard.TargetProperty="Opacity"
                                                         To="0.3" Duration="0:0:0.2"/>

                                        <ColorAnimation Storyboard.TargetName="bg"
                                                        Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                        To="#1E000000" Duration="0:0:0.2"/>

                                        <DoubleAnimation Storyboard.TargetName="txt"
                                                         Storyboard.TargetProperty="Opacity"
                                                         To="0" Duration="0:0:0.2"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </MultiTrigger.ExitActions>
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>



    <Style x:Key="general" TargetType="Button">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="bg" Background="#1E000000" Height="140" Width="160">
                        <Grid>
                            <Image x:Name="icon" Height="70" Width="70" Opacity="0.3" VerticalAlignment="Center" HorizontalAlignment="Center" Source="/Resources/Usercontrols/Settings/Images/[settings]-General.png"/>
                            <TextBlock x:Name="txt" Text="GENERAL" Foreground="#fff" FontSize="16" FontWeight="Light" Opacity="0" VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0,0,0,16"/>
                        </Grid>
                    </Border>

                    <ControlTemplate.Triggers>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True"/>
                            </MultiTrigger.Conditions>
                            <MultiTrigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="icon"
                                                     Storyboard.TargetProperty="Opacity"
                                                     To="1" Duration="0:0:0.2"/>

                                        <ColorAnimation Storyboard.TargetName="bg"
                                                    Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                    To="#4C000000" Duration="0:0:0.2"/>

                                        <DoubleAnimation Storyboard.TargetName="txt"
                                                     Storyboard.TargetProperty="Opacity"
                                                     To="1" Duration="0:0:0.2"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </MultiTrigger.EnterActions>
                            <MultiTrigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="icon"
                                                     Storyboard.TargetProperty="Opacity"
                                                     To="0.3" Duration="0:0:0.2"/>

                                        <ColorAnimation Storyboard.TargetName="bg"
                                                    Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                    To="#1E000000" Duration="0:0:0.2"/>

                                        <DoubleAnimation Storyboard.TargetName="txt"
                                                     Storyboard.TargetProperty="Opacity"
                                                     To="0" Duration="0:0:0.2"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </MultiTrigger.ExitActions>
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>



    <Style x:Key="security" TargetType="Button">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="bg" Background="#1E000000" Height="140" Width="160" CornerRadius="0,5,0,0">
                        <Grid>
                            <Image x:Name="icon" Height="70" Width="70" Opacity="0.3" VerticalAlignment="Center" HorizontalAlignment="Center" Source="/Resources/Usercontrols/Settings/Images/[settings]-Security.png"/>
                            <TextBlock x:Name="txt" Text="SECURITY" Foreground="#fff" FontSize="16" FontWeight="Light" Opacity="0" VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0,0,0,16"/>
                        </Grid>
                    </Border>

                    <ControlTemplate.Triggers>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True"/>
                            </MultiTrigger.Conditions>
                            <MultiTrigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="icon"
                                                     Storyboard.TargetProperty="Opacity"
                                                     To="1" Duration="0:0:0.2"/>

                                        <ColorAnimation Storyboard.TargetName="bg"
                                                    Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                    To="#4C000000" Duration="0:0:0.2"/>

                                        <DoubleAnimation Storyboard.TargetName="txt"
                                                     Storyboard.TargetProperty="Opacity"
                                                     To="1" Duration="0:0:0.2"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </MultiTrigger.EnterActions>
                            <MultiTrigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="icon"
                                                     Storyboard.TargetProperty="Opacity"
                                                     To="0.3" Duration="0:0:0.2"/>

                                        <ColorAnimation Storyboard.TargetName="bg"
                                                    Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                    To="#1E000000" Duration="0:0:0.2"/>

                                        <DoubleAnimation Storyboard.TargetName="txt"
                                                     Storyboard.TargetProperty="Opacity"
                                                     To="0" Duration="0:0:0.2"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </MultiTrigger.ExitActions>
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>



    <Style x:Key="appearance" TargetType="Button">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="bg" Background="#1E000000" Height="140" Width="160" CornerRadius="0,0,0,5">
                        <Grid>
                            <Image x:Name="icon" Height="70" Width="70" Opacity="0.3" VerticalAlignment="Center" HorizontalAlignment="Center" Source="/Resources/Usercontrols/Settings/Images/[settings]-Appearance.png"/>
                            <TextBlock x:Name="txt" Text="APPEARANCE" Foreground="#fff" FontSize="16" FontWeight="Light" Opacity="0" VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0,0,0,16"/>
                        </Grid>
                    </Border>

                    <ControlTemplate.Triggers>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True"/>
                            </MultiTrigger.Conditions>
                            <MultiTrigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="icon"
                                                         Storyboard.TargetProperty="Opacity"
                                                         To="1" Duration="0:0:0.2"/>

                                        <ColorAnimation Storyboard.TargetName="bg"
                                                        Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                        To="#4C000000" Duration="0:0:0.2"/>

                                        <DoubleAnimation Storyboard.TargetName="txt"
                                                         Storyboard.TargetProperty="Opacity"
                                                         To="1" Duration="0:0:0.2"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </MultiTrigger.EnterActions>
                            <MultiTrigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="icon"
                                                         Storyboard.TargetProperty="Opacity"
                                                         To="0.3" Duration="0:0:0.2"/>

                                        <ColorAnimation Storyboard.TargetName="bg"
                                                        Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                        To="#1E000000" Duration="0:0:0.2"/>

                                        <DoubleAnimation Storyboard.TargetName="txt"
                                                         Storyboard.TargetProperty="Opacity"
                                                         To="0" Duration="0:0:0.2"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </MultiTrigger.ExitActions>
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>



    <Style x:Key="advanced" TargetType="Button">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="bg" Background="#1E000000" Height="140" Width="160">
                        <Grid>
                            <Image x:Name="icon" Height="70" Width="70" Opacity="0.3" VerticalAlignment="Center" HorizontalAlignment="Center" Source="/Resources/Usercontrols/Settings/Images/[settings]-Advanced.png"/>
                            <TextBlock x:Name="txt" Text="ADVANCED" Foreground="#fff" FontSize="16" FontWeight="Light" Opacity="0" VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0,0,0,16"/>
                        </Grid>
                    </Border>

                    <ControlTemplate.Triggers>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True"/>
                            </MultiTrigger.Conditions>
                            <MultiTrigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="icon"
                                                     Storyboard.TargetProperty="Opacity"
                                                     To="1" Duration="0:0:0.2"/>

                                        <ColorAnimation Storyboard.TargetName="bg"
                                                    Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                    To="#4C000000" Duration="0:0:0.2"/>

                                        <DoubleAnimation Storyboard.TargetName="txt"
                                                     Storyboard.TargetProperty="Opacity"
                                                     To="1" Duration="0:0:0.2"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </MultiTrigger.EnterActions>
                            <MultiTrigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="icon"
                                                     Storyboard.TargetProperty="Opacity"
                                                     To="0.3" Duration="0:0:0.2"/>

                                        <ColorAnimation Storyboard.TargetName="bg"
                                                    Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                    To="#1E000000" Duration="0:0:0.2"/>

                                        <DoubleAnimation Storyboard.TargetName="txt"
                                                     Storyboard.TargetProperty="Opacity"
                                                     To="0" Duration="0:0:0.2"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </MultiTrigger.ExitActions>
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>



    <Style x:Key="feedback" TargetType="Button">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="bg" Background="#1E000000" Height="140" Width="160" CornerRadius="0,0,5,0">
                        <Grid>
                            <Image x:Name="icon" Height="70" Width="70" Opacity="0.3" VerticalAlignment="Center" HorizontalAlignment="Center" Source="/Resources/Usercontrols/Settings/Images/[settings]-Feedback.png"/>
                            <TextBlock x:Name="txt" Text="FEEDBACK" Foreground="#fff" FontSize="16" FontWeight="Light" Opacity="0" VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0,0,0,16"/>
                        </Grid>
                    </Border>

                    <ControlTemplate.Triggers>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True"/>
                            </MultiTrigger.Conditions>
                            <MultiTrigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="icon"
                                                 Storyboard.TargetProperty="Opacity"
                                                 To="1" Duration="0:0:0.2"/>

                                        <ColorAnimation Storyboard.TargetName="bg"
                                                Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                To="#4C000000" Duration="0:0:0.2"/>

                                        <DoubleAnimation Storyboard.TargetName="txt"
                                                 Storyboard.TargetProperty="Opacity"
                                                 To="1" Duration="0:0:0.2"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </MultiTrigger.EnterActions>
                            <MultiTrigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="icon"
                                                 Storyboard.TargetProperty="Opacity"
                                                 To="0.3" Duration="0:0:0.2"/>

                                        <ColorAnimation Storyboard.TargetName="bg"
                                                Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                To="#1E000000" Duration="0:0:0.2"/>

                                        <DoubleAnimation Storyboard.TargetName="txt"
                                                 Storyboard.TargetProperty="Opacity"
                                                 To="0" Duration="0:0:0.2"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </MultiTrigger.ExitActions>
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>